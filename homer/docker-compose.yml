---
version: "3.7"
services:
  homer:
    image: b4bz/homer
    container_name: homer
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
    volumes:
      - ${FILE1}:/www/assets
    networks:
      - proxy
    ports:
      - 8080:8080
    restart: unless-stopped


networks:
  proxy:
    external: true